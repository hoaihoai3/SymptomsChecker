import React from 'react';
import { View, Text } from 'react-native';
import { Info } from './common';

const InfoPage = ({ title, detail }) => {
  return (
    <Info />
  )
}

export default InfoPage;
